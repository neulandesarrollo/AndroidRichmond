<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.neuland.richmond">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:name="android.support.multidex.MultiDexApplication"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="android.app.searchable"
            android:resource="@xml/searchable" />

        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".MainMenuActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".ListaSerieActivity"
            android:theme="@style/preschool" />
        <activity
            android:name=".DetallesSerieActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".InicialActivity"
            android:theme="@style/Inicial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapaActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".ListaSeriePrimaryActivity"
            android:theme="@style/primary" />
        <activity
            android:name=".ListaSerieSecondaryActivity"
            android:theme="@style/secondary" />
        <activity
            android:name=".ListaSerieYoungActivity"
            android:theme="@style/young" />
        <activity
            android:name=".ListaSerieSupplementaryActivity"
            android:theme="@style/supplementary" />
        <activity
            android:name=".ListaSerieExamsActivity"
            android:theme="@style/exams" />
        <activity
            android:name=".ListaSerieDigitalActivity"
            android:theme="@style/digital" />
        <activity
            android:name=".ListaSerieReadersActivity"
            android:theme="@style/readers" />
        <activity
            android:name=".DetallePrimaryActivity"
            android:theme="@style/primary" />
        <activity
            android:name=".DetalleSecondaryActivity"
            android:theme="@style/secondary" />
        <activity
            android:name=".DetalleYoungActivity"
            android:theme="@style/young" />
        <activity
            android:name=".DetalleSupplementaryActivity"
            android:theme="@style/supplementary" />
        <activity
            android:name=".DetalleExamsActivity"
            android:theme="@style/exams" />
        <activity
            android:name=".DetalleReadersActivity"
            android:theme="@style/readers" />
        <activity
            android:name=".SearchActivity"
            android:theme="@style/SplashTheme">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".ListaEstadosActivity"
            android:theme="@style/SplashTheme"></activity>
        <activity android:name=".DetalleEstadoActivity"
            android:theme="@style/SplashTheme">
        </activity>
    </application>

</manifest>